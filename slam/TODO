testing in general

use another particle swarm to find 6 circles (x, y of center and radius) that are as small as possible, but cover the maximum amount of known obstacles from overall map

save a rescue overall map to revert to if overall map becomes noisy

